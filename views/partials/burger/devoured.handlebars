<h2> Burger Devoured </h2>
<br/>
<ul>
  {{#each burgers}}
  	{{#if this.devoured}}
     <li>
		<span id="devouredBox">
        	{{this.burger_name}}
        </span>
     </li> 
    {{/if}}
  {{/each}}
</ul>